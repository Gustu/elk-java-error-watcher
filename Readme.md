#Error Woofer

### Usage

To start copy .env files using command:

`make build`

Then run Docker containers:

`make run-all`

### Development

1. `make build` 
2. `make run-docker`
3. `make run-watcher`